import React from "react";
import PropTypes from "prop-types";
import { Container, Row, Col } from "react-grid-system";

const MainScreen = () => {
	return <>MainScreen created.</>;
};

MainScreen.propTypes = {};

export default MainScreen;